;;; init-matrix.el --- Config matrix client(Emacs) -*- lexical-binding: t -*-

;;; Commentary:
;;

;;; Code:

(use-package ement
  :ensure t
  :defer t
  :custom
  (ement-save-sessions t)
  (ement-room-list-side-window t)
  )

(provide 'init-matrix)
;;; init-matrix.el ends here.
